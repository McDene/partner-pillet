---
import BaseLayout from '../layouts/BaseLayout.astro';
import Header from '../components/Header.astro';
import ServicePage from '../components/ServicePage.astro';
import FinalCta from '../components/FinalCta.astro';
import Footer from '../components/Footer.astro';
import data from '../content/services/transmission-gouvernance.json';
---

<BaseLayout title={data.seo.title} description={data.seo.description}>
  <Header />
  <main>
    <ServicePage
      eyebrow={data.eyebrow}
      title={data.title}
      intro={data.intro}
      situationsTitle={data.situationsTitle}
      situations={data.situations}
      servicesTitle={data.servicesTitle}
      services={data.services}
      resultsTitle={data.resultsTitle}
      results={data.results}
    />
    <FinalCta
      title="Vous préparez une transmission ou une évolution de gouvernance ?"
      text="Ces étapes méritent une préparation rigoureuse. Parlons-en dans le cadre d'un premier échange confidentiel."
      cta={{ label: "Planifier un entretien confidentiel", href: "/contact" }}
    />
  </main>
  <Footer />
</BaseLayout>
